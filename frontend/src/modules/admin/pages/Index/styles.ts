import styled from "styled-components";

export const Container = styled.div``;

export const HeadContainer = styled.div``;

export const Title = styled.h3`
  color: ${({ theme }) => theme.colors.headlineColor };
  font-size: 2.8rem;
`;